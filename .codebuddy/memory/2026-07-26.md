# 2026-07-26 工作记录

## Mock MQTT 信封签名修复
- signingKey 从 activate/verify 获取后从未存储，login 等上行消息 `sign: ''`
- 修复：mqtt-sim.js 新增 `setSigningKey()`、`sendEnvelope()`(含 HMAC-SHA256)、`computeSign()`；心跳改用 `card/{dc}/heartbeat` + 信封格式
- bridge.js `setCredentials` 新增 signingKey 参数；service.js 4处存储+使用 signingKey
- 移除模拟 loginResp，等真实服务端响应

## Mock 模式页面刷新强制回到 splash
- 问题：浏览器刷新保留 URL hash，跳过 bootstrap
- 修复：vite.config.js 注入 `__CARD_MOCK_DEV__`；main.js 在最顶部检测并 `location.replace` 到 splash

## Mock 模式 bootstrap.deviceInfo 和 MQTT 状态推送缺失
- `bootstrap.deviceInfo`: bridge.js 的 request switch 无 `'bootstrap'` case → 返回 null；新增 `handleBootstrap()` 处理 `deviceInfo`
- `socket.statusChanged`: MQTT 连接/断开/重连时从未 emit；mqtt-sim.js `createMqttClient` 新增第6参数 `onStatusChange`，在 connect/close/offline/reconnect/error 时回调
- bridge.js 和 service.js 均传递 `onStatusChange` → `emit('socket.statusChanged', status)`

## Mock MQTT 连接状态推送修复
- `service.js` 的 `setupMqtt` 原来直接 import `mqtt-sim.js` 创建独立客户端，绕过 `bridge.js`
- 导致 `bridge.js` 的 `mqttClient` 为 null，`device.info` 事件从不推送，`loginStatus` 返回 disconnected
- 修复：`bridge.js` 导出 `connectMqtt`，返回 Promise（channel ready 时 resolve）；`service.js` 的 `setupMqtt` 改为 `return connectMqtt()`
- 同时修复 `emitDeviceInfo()` 中的 `setDeviceInfo(data)` 污染 bug（移除该行）
- 同时让 `mock/service.js` 的 `bootstrapDeviceInfo()` 委托 `bridgeRequest('bootstrap.deviceInfo')` 而非硬编码

## Mock MQTT clientId 和 bootstrap 响应补充
- `connectMqtt` 固定传 `null` clientId → mqtt-sim 兜底生成 `browser_${dc}` → broker 鉴权失败 "Not authorized"
- 修复：`connectMqtt({ clientId })` 透传激活响应中的真实 clientId
- `handleBootstrap.deviceInfo` 缺少 `mqttConnected` 字段 → 新增 `mqttConnected: isConnected()`
- 至此 mock 的 bootstrap、deviceInfo、device.info、MQTT 连接/鉴权/登录流程与 Android 行为对齐

## 今日 Mock 工作完成
- 修改文件：`uniapp/src/mock/bridge.js`、`uniapp/src/mock/service.js`、`uniapp/src/mock/mqtt-sim.js`
- 对齐项：设备信息来源统一、MQTT 连接推送 `device.info`、`socket.statusChanged`、信封签名、clientId 透传、bootstrap 响应含 `mqttConnected`
- 构建验证：全部 PASS

## 本机管理员权限模型设计 V2
- 基于 `docs/本地管理员模式设计.md` 原有三角色模型，完善为可自定义的 RBAC 系统
- 新增能力：角色 CRUD、权限自定义、多角色凭据（N:M）、角色继承链 + 权限层级展开
- 完整数据模型：`local_roles`(parent_role_id 继承)、`local_permissions`(parent_key 层级)、`local_role_permissions`、`local_auth_credentials`、`local_credential_roles`(N:M)、`local_auth_session`
- 登录流程：密码 → PBKDF2 匹对凭据 → 查多角色 → 递归展开角色链+权限树 → effectivePermissions Set → appState.session
- 页面守卫：全局路由拦截器 + 页面权限映射表，首页/启动页匿名，其余需权限
- 动作级二次校验：所有危险操作（弹卡/重启/升级等）service 层 assertPermission
- 防锁死：最后一个 admin.manage 凭据不可删除；is_system 角色不可删除；角色保存时检测继承环
- 7 阶段实施计划 + 8 项风险缓解 + 5 项待确认（凭据数量上限、默认密码策略、远程/本地认证关系、TTL、admin.manage 授权范围）
- 当前代码 ctx：PERMISSIONS/ROLE 常量已定义但 session 从未赋值 → 权限系统已定义未生效

## 本机管理员模式文档 — 待确认项全部落地

用户对 `docs/本地管理员模式设计.md` 中 5 项待确认给出了明确决策，文档已同步更新：

| # | 决策 | 文档改动位置 |
|---|------|------------|
| 1 | 凭据数量不设上限，理论上限 10⁶ | 2.2 冲突说明、16 已确认决策 |
| 2 | 允许简单密码，首次登录强制修改 | 5 密码规则（新增强制修改流程）、7.4 首次登录强制修改密码 |
| 3 | 远程 `/auth/login` 不存在，移除 | 5 密码规则（改写为明确声明不存在）、16 已确认决策 |
| 4 | TTL 需要，创建凭据时设默认值；页面跳转刷新 TTL；启动即清保留 | 4.4 `local_auth_credentials` 新增 `ttl_seconds` 字段、4.7 session 表 `expires_at` 改为 NOT NULL、7 TTL 会话管理（全新章节：7.1-7.4） |
| 5 | `admin.manage` 需要二级密码，写死 `123456`，后续从 config 接口 `secondaryPassword` 字段获取 | 12.4 二级密码（新增） |

同时：**选用方案 A**（全局路由拦截）替代 mixin 方案；新增 section 7 "TTL 会话管理与强制修改密码" 导致章节号全部 +1；所有子节编号已修复。种子数据补充了 `ttl_seconds=3600` 和 `password_state='INITIAL'`。

## 本机管理员文档二次调整（2角色 + 二级密码本地化）

用户对初始化角色和二级密码提出进一步决策：

1. **二级密码本地化**：
   - 新增 `local_secondary_password` 表（§4.8），永远 1 行存储 PBKDF2 hash + salt
   - 默认 `123456`，支持在管理页本地修改；不再依赖 config 接口
   - §12.4 校验流程改为查表比对，§16 决策 5 同步更新

2. **2 内置角色（不可删除）**：
   - 删除 OPS 角色，保留 SUPER_ADMIN + DEVELOPER
   - 超级管理员：密码 `123456`，激活成功后写入（§11.3 新增初始化时序），password_state='ACTIVE'，再次激活不覆盖
   - 开发人员：密码从 config 接口 `adminPassword` 字段下发，每次 config 调用都可能同步更新（§11.3 新增同步逻辑）
   - §11.1 种子数据更新为 2 角色；§2.2 冲突说明同步调整

## 二级密码特权角色豁免
- SUPER_ADMIN 和 DEVELOPER 为系统内置特权角色，跳过二级密码校验直接进入管理页
- 只有通过自定义角色获得 `admin.manage` 权限的凭据才需要二次校验
- 特权角色列表硬编码：`['SUPER_ADMIN', 'DEVELOPER']`，不可通过角色管理修改
- §12.4 校验流程更新为：先检查特权角色 → 是则跳过 → 否则弹二级密码输入框
