# 2026-07-28

## faceai-server 部署与接口完善
- 在 CentOS 7 宝塔环境下成功部署 faceai-server
- 解决了 4 个关键问题：
  1. pritunl 仓库 404 → `yum-config-manager --disable pritunl`
  2. Mac node_modules 拷到 Linux 报 `invalid ELF header` → 删掉在服务器上重装
  3. 宝塔 Node 不在 PATH → `export PATH="/www/server/nodejs/v14.20.1/bin:$PATH"`
  4. GLIBC 2.17 太低 → `npm install --build-from-source` 本地编译
- 新增 5 个 API 接口到 `server.js`：
  - POST /api/face/enroll/batch — 批量录入
  - DELETE /api/face/enroll/:employeeId — 删除指定员工
  - GET /api/face/db/status — 查看人脸库详情
  - GET /api/face/db/export — 导出人脸库（JSON, 特征 base64）
  - POST /api/face/db/import — 导入人脸库（JSON）
- 完善 `DEPLOY.md`：更新接口表、新增验证示例、持久化工作流章节、FAQ
- 完善 `face-service.js`：新增 faceBatchEnroll、faceDeleteEmployee、faceGetDbStatus、faceExportDb、faceImportDb 公共 API
- **生产 URL 已确认**：`http://card-test.quyohui.com/faceapi/`（Nginx 反向代理 → `127.0.0.1:3456`）
- `config.js` FACE_SERVICE_URL 更新为 `http://card-test.quyohui.com/faceapi`
- `DEPLOY.md` 补充代理路径 `/faceapi/` 说明
- **修复 `face-service.js` startCamera 竞态条件**：旧代码 retry fire-and-forget，不等 video 元素出现和播放就返回 true，导致 captureFromVideo 返回 null。新增 `waitForElement` + `waitForVideoReady`（loadedmetadata 事件）确保 camera 真正就绪后再截图
- **修复 mock 模式密码修改报错**：`localStore.js` `changeLocalCredentialPassword` 中 UPDATE 语句混用 `?` 占位符和字面值（`failed_count = 0, locked_until = 0`），mock 的 `worker-adapter.js` parseUpdate 按逗号分隔 SET 不区分是否为 `?`，导致参数索引错位，WHERE `credential_id = ?` 拿不到正确参数，UPDATE 实际影响 0 行，verify SELECT 发现 password_state 未变为 ACTIVE 而报错。修复为全部使用 `?` 占位符并补齐参数
- **修复 mock 模式串口一直显示 DISCONNECTED**：三处联动修复 — (1) `mock/data.js` `defaultRuntime.serial` 从 DISCONNECTED 改为 CONNECTED；(2) `mock/bridge.js` init 后 emit `serial.statusChanged` CONNECTED 事件，`handleSerial('getLogs')` 返回模拟日志（不再空数组），新增 `status`/`listPorts` 处理器；(3) `mock/serial-sim.js` 新增收发字节追踪、mock 日志管理（pushMockLog/getMockLogs/getSerialTxBytes/getSerialRxBytes）、start() 时推送 STATUS 日志保证 `getSerialStatus()` 识别已连接

## serial-demo.vue 纯串口调试页优化
- 移除后端/MQTT/HTTP 状态展示区（backend-line）、`backendStatus` reactive、socket/sync/slotsSnapshot 事件监听 — 这些与串口调试无关
- 移除导入 `replaceSlotsProjection`（仅用于已删除的 slotsSnapshot 监听）
- **所有 slot 控制按钮改为直接 `services.sendSerial(hex, 'HEX')` 发送 HEX 帧**，不再走 broken stub（`services.takeCard`/`returnCard`/`querySlot`/`readBoardVersion` 全部是 `unsupportedClientFeature`）
- `ejectAllCards` 改为直接遍历 boardAddresses 逐帧发送，不再调用 broken `services.unlockAllDoors()`
- `exitAdmin` 改为纯导航返回（`navigateBack`），不再调用 `services.logout()` + `reLaunch`
- 移除死代码：`sendProtocolCommand`、`waitForSerialResponse`、`waitMs`、`toHexByte`
- 清理 9 行 backend-line 相关 CSS 和 responsive media query 中的 2 行 backend 规则
- 状态增加"轮询中"指示
- 文件从 582 行缩减到 527 行
- **串口日志过滤 CONNECTED 消息**：`statusChanged` 事件中 `state === 'CONNECTED'` 不再写入终端日志，仅更新顶部状态条，避免日志被 CONNECTED 消息刷屏
- **移除还卡开门按钮**，取卡拆分为"管理员取卡"（primary 绿）和"普通取卡"（outline 浅绿），控制区新增"LED亮度调整"（info 蓝）弹窗选 1-30 亮度值后发 `0x54` 指令
- **按钮分三大类**：控制（取卡/弹卡/LED）、查询（卡状态/版本）、OTA（占位禁用），每类用 `.action-label` 分隔
- **右上角控制面板开关**：新增 `showPanels` ref（默认 false）+ `.panel-toggle` iOS 风格开关，勾选后才显示串口配置、卡槽控制、手动命令三个 section，未勾选时显示引导提示
- **按钮宽度统一**：`.action-button` 新增 `min-width: 0` 确保 2 列 grid 内按钮均分宽度
- 新增 `.top-bar-right` flex 容器容纳 toggle + status-pill；新增 toggle 和 panel-empty CSS
- **control-panel 默认隐藏**：`v-if="showPanels"` 提到 `control-panel` div 上，关闭时 `console-layout` 自动切换为 `single-column`（1fr），终端撑满全宽
- **管理界面页面层级分析文档**：创建并更新 `docs/管理界面页面层级分析-2026-07-28.md`，包含 17 页面导航树、4 个层级问题诊断、7 大功能类别分类，以及**已决策的 5 项调整方案**（含逐项旧→新对比、变化原因、更新后完整导航树）。5 项决策：A. management+engineering 合并为工程模式（删 management、增强 engineering）；B. admin 仪表盘按「账号权限/系统设置/设备维护/实时状态」4 分区；C. 删除 feature?type=password，change-password.vue 视觉升级对齐 feature modern 风格（无边框输入框、clamp 尺寸、圆形图标+标题）；D. system.vue 按「生物识别录入/数据管理/系统工具」3 二级分组，移除密码和工程模式入口；E. 目录整理删 management
- **全部 5 项调整已实施完成**：
  1. 删除 `management.vue` + 空目录，`engineering.vue` 重写合并 management 的权限过滤（`hasPermission`）、ModalShell（解锁确认/升级/信息弹窗）、全部 7 项菜单
  2. `admin.vue` 重写为 4 分区布局（section+section-title+section-grid），新增「修改密码」入口（`auth.password.manage`），「管理功能」替换为「工程模式」（`debug.command` gate）
  3. `change-password.vue` 视觉全面升级：大圆形 lock 图标（黄色 #ffc95b）、大号标题、无边框 clamp 输入框（h:50-66px、bg:#f4f7fb、r:12px）、primary-gradient-button
  4. `system.vue` 重写为 3 分区（生物识别录入/数据管理/系统工具），移除修改密码和工程模式入口，从 10 卡片减为 9 卡片
  5. `feature.vue` 删除 `type=password` 模板分支，剩余 4 个 type（restart/authorization/units/history）
  6. 清理引用：`main.js` PAGE_PERMISSIONS 移除 management 路由和 admin 的 management.menu；`localStore.js` DEFAULT_OPERABLE_ITEMS 移除 management.menu；`src/pages.json` 和 `uniapp/pages.json` 均移除 management 路由
  7. 权限审查结论：无需调整种子数据。`management.menu` 保留在 SEED_PERMISSIONS 中以保持向后兼容，UI 中不再使用。`debug.command` 作为工程模式新 gate（DEVELOPER 已有此权限）。H5 构建因 `uniapp-发卡机-client` 中文包名导致 npm 报错（预存问题），可绕过用 `npx uni build`

## 第二轮调整：admin.vue 选项卡式全局仪表盘（2026-07-28 晚）

用户反馈第一版 admin（4 分区卡片 → navigateTo 子页面）"布局很奇怪，不美观"，要求改成"选项卡的样式，直接把每个分类直接展示出来，而不是进入二级界面，/admin/admin 就应该是一个全局的后台功能导航功能，不在进入后又一个二级导航"。

### 实施

**admin.vue 完全重写为选项卡式单页仪表盘**：

```
┌─────────────────────────────────────────┐
│ AdminHeader [角色] [退出管理]             │
├─────────────────────────────────────────┤
│ █ 账号权限 │ 系统设置 │ 设备维护 │ 实时状态 │  ← Tab 栏（蓝底滑动指示器）
├─────────────────────────────────────────┤
│ scroll-view 内联全部当前 Tab 内容         │
└─────────────────────────────────────────┘
```

**4 个 Tab 的内容**：
- **账号权限**：修改密码表单 + 角色管理（双栏：左列表/右表单+权限chips）+ 用户管理（双栏：左列表/右表单+角色chips+二级密码修改面板），全部内联。角色/用户区域首次需二级密码验证（PasswordModal），通过后显示。使用正确的 localStore 方法名（`saveLocalRole`、`saveLocalCredential`、`deleteLocalRole`、`deleteLocalCredential`、`unlockLocalCredential`、`listLocalRoles`、`listLocalCredentials`）。
- **系统设置**：3 个二级分组卡片网格（生物识别/数据管理/系统工具），点击卡片 navigateTo 到深层功能页面。
- **设备维护**：7 项工程模式菜单 + 升级弹窗（`getUpgradeFiles` → 选文件 → `startUpgrade(fileId, cb)`）+ 一键弹出确认弹窗（`unlockAllDoors`）+ 硬件版本号弹窗（`getRuntime`）。
- **实时状态**：10×6 卡位网格（`appState.slots` 排序）+ StatusLegend × 2 + SlotDetailModal（`unlockDoor(slotNumber)`）。

**关键服务方法对接**：
- 使用 `import { services } from '@/services/index.js'`（具名导出）
- 升级：`services.getUpgradeFiles()` → `services.startUpgrade(fileId, progressCb)`
- 弹门：`services.unlockAllDoors()` / `services.unlockDoor(slotNumber)`
- 退出：`services.logout()` → `uni.reLaunch('/pages/index/index')`
- 密码：`services.changeLocalPassword(old, new)`
- 角色表单字段：`roleId`, `roleName`, `permissionKeys`（对齐 `saveLocalRole` 契约）
- 用户表单字段：`credentialId`, `label`, `roleIds`, `password`, `enabled`（对齐 `saveLocalCredential` 契约）
- 二级密码：`services.changeSecondaryPassword(old, new)`

**Tab 视觉**：白色胶囊背景（r=14px）、Tab 标签带 IconGlyph + 文字、激活 Tab 蓝色字 + 底部蓝色滑动指示器（`transform .28s cubic-bezier`）。

**文档更新**：`docs/管理界面页面层级分析-2026-07-28.md` 新增第八章「第二轮调整」，含结构图、4 个 Tab 内容表、设计要点、子页面状态说明、旧→新对比表。

**验证**：lint 零错误；JS 语法检查（ES module 预存问题）；H5 构建（vite.config.js `import` 语法需 `"type": "module"` — 预存问题，与本次无关）。

## 修复账号权限 Tab 样式混乱（2026-07-28 晚）

用户反馈"权限管理的tab样式混乱"。问题根因：
- 列表行无分隔线、字体 10-13px 过小
- 表单输入框 36px 高、无边框
- `max-height:440px` 造成嵌套滚动
- chips/标签 10-11px 难以阅读和点击
- 左侧列表灰色背景显得脏乱

**全面修复**（仅 CSS，未改模板结构）：
- 列表列：白底 + `border` 替代灰底，行加 `border-bottom` 分隔，移除 `max-height`，名称 14px / 编码 12px
- 表单列：输入框 40px + 可见边框 + focus 蓝色高亮，标签 62px 宽 / 13px 粗体，chips 11-13px + 扩展 padding
- 卡片头部：`padding-bottom` + `border-bottom` 分隔线
- 按钮：统一 32-42px 高，13-14px 字号
- 锁定区域：padding 32px，文字 14px
- badge：8→10px；双栏 gap：12→16px；各间距和圆角统一放大

**验证**：lint 零错误；真机未验证。

## 第三轮调整：Tab 内部改为卡片导航 + 弹窗模式（2026-07-28 晚）

用户澄清需求：admin 的 Tab 不是内联展开所有表单，而是展示 Icon 卡片导航，点击卡片弹出功能窗口。类似 系统设置 Tab 的 AdminMenuCard 网格模式。

**关键决策**：
- 每个 Tab 内用 `sys-grid`（`grid-template-columns: repeat(auto-fill, minmax(138px, 1fr))`）排列 AdminMenuCard
- 点击卡片打开对应弹窗（ModalShell），内容在弹窗内处理
- 角色管理/用户管理/卡位状态使用 `modal-wide` 大弹窗（`width: min(88vw, 720px)`）

**四个 Tab 的卡片布局**：
- **账号权限**：角色管理（蓝紫 #5965E9）+ 用户管理（绿 #00A86B）+ 修改密码（黄 #F59E0B）
- **系统设置**：不变（三级分组卡片网格）
- **设备维护**：不变（7 项线框卡片 + 弹窗）
- **实时状态**：卡位实时状态（蓝 #1F76FF）+ MQTT通信状态（灰 #A0B0C4，线框变体，无点击，预留占位）

**弹窗内容**：
- **修改密码**：ModalShell 普通弹窗，3 个 input + 保存按钮
- **角色管理**：ModalShell modal-wide，双栏 CRUD（左列表/右表单+权限chips）；首次需二级密码验证
- **用户管理**：ModalShell modal-wide，双栏 CRUD（左列表/右表单+角色chips+二级密码修改面板）；首次需二级密码验证
- **卡位状态**：ModalShell modal-wide，10×6 SlotCard 网格 + StatusLegend × 2 + SlotDetailModal 浮层
- **工程操作用**：一键弹出/升级/硬件信息，ModalShell 普通弹窗，内容迁入弹窗
- **二级密码验证**：PasswordModal 独立弹窗

**代码结构变化**：
- 模板：Tab 内容从内联表单 → 卡片网格；功能内容从内联 → Modals 区域
- 脚本：新增 `showRoleModal/showCredentialModal/showPasswordForm/showSlotStatusModal` 和对应的 open 方法；`openRoleModal/openCredentialModal` 自动检查 `adminManageReady` 并触发二级密码验证
- 样式：新增 `:deep(.modal-wide)` 宽模态样式；`modal-content`/`modal-title` 作为弹窗内容标准布局；双栏/列表/表单样式保留复用于弹窗内部

**验证**：lint 零错误；真机未验证。

## 修复：AdminHeader 显示 builtin:SUPER_ADMIN 而非用户名（2026-07-28 晚）

- 根因：`userLabel = computed(() => appState.session?.displayName || appState.session?.credentialId || '')`；
  session 有 `credentialLabel`（"超级管理员"）但取值链未包含，`displayName` 不存在 → 回退到 `credentialId`（"builtin:SUPER_ADMIN"）
- 修复：取值链改为 `session?.credentialLabel || session?.displayName || session?.credentialId`
- lint 零错误

## 第四轮调整：角色管理/用户管理改为独立页面跳转（2026-07-28 晚）

用户要求：角色管理和用户管理不应弹出窗口，而是页面跳转；修改密码保持弹窗。

**变更概要**：

管理员3个文件，其中2个为新增：
- `uniapp/src/pages/admin/admin.vue`（修改）— 移除 role/cred 模态，卡片改为 `uni.navigateTo`，清理 ~250 行无用代码（模板+脚本+样式）
- `uniapp/src/pages/admin/role-manage.vue`（**新建**）— 独立角色 CRUD 页面（AdminHeader+AdminPageToolbar+双栏+二级密码保护）
- `uniapp/src/pages/admin/credential-manage.vue`（**新建**）— 独立用户 CRUD 页面（AdminHeader+AdminPageToolbar+双栏+二级密码保护+二级密码修改面板）

**路由**：`pages.json` 中已存在，无需修改。

**admin.vue 清理**：
- 移除：→ 角色/用户 ModalShell 模板，→ UiSwitch/PasswordModal 导入，→ 角色/用户/凭证/二级密码/权限 全套响应式数据和函数（~200行），→ 双栏/列表列/表单列/锁定/chip 等样式
- 卡片点击从 `@click="openRoleModal"` 改为 `@click="go('/pages/admin/role-manage')"`

**验证**：lint 零错误（全部 3 个文件）；真机未验证。

## 第五轮调整：系统设置→系统管理 + 弹窗/跳转分离 + 角色权限显示修复（2026-07-28 晚）

### 1. Tab 重命名与交互模式分离
- Tab `系统设置` → `系统管理`
- 系统管理 Tab 下的卡片根据 `interaction` 字段区分交互模式：
  - `interaction: 'modal'` → 点击展开弹窗（`systemModal` 状态驱动对应 ModalShell）
  - 无 `interaction`（有 `route`）→ `uni.navigateTo` 页面跳转
- 弹窗交互项：人脸注册、指纹注册、系统授权、设备设置
- 页面跳转项：人员管理、单元管理、历史管理、重启应用

### 2. 新增 4 个系统管理 ModalShell 弹窗
- **人脸注册**：员工编号输入 + `registerBiometric('FACE', ...)` 按钮 + 状态反馈
- **指纹注册**：同上，使用 `registerBiometric('FINGERPRINT', ...)`
- **系统授权**：显示激活状态/设备编号/激活时间 + "重启激活流程" 按钮 → `restartApp()`
- **设备设置**：9 个关键字段表单（卡柜号/卡位总数/设备编号/HTTP主机端口/MQTT主机端口/串口路径/波特率），可滚动的 `modal-wide` 弹窗
- 新增 `onSysItem(item)` 分发函数、`loadAuthStatus()`/`loadConfigForm()`/`doSaveConfig()` 等操作函数

### 3. 角色管理权限显示 BUG 修复
- **根因**：模板使用 `p.code` 和 `p.label` 访问权限数据，但 `listLocalPermissions()` 返回的字段是 `permissionKey` 和 `permissionName`
- **修复**：
  - `p.code` → `p.permissionKey`，`p.label` → `p.permissionName`
  - 新增 `groupedPermissions` computed：按 `category`（模块）分组
  - 模板改为两层循环：外层遍历分组（显示 `perm-cat` 模块标题），内层遍历组内权限 chips
  - 新增 `perm-scroll`/`perm-group`/`perm-cat` 样式实现模块分层排布

**涉及文件**：
- `uniapp/src/pages/admin/admin.vue`（修改：Tab 标签 + 系统 Item 交互标记 + 4 个弹窗 + 分发逻辑 + 样式）
- `uniapp/src/pages/admin/role-manage.vue`（修改：权限字段修正 + 分组显示 + 样式）

**验证**：lint 零错误（2 个文件）；JS 语法校验通过；真机未验证。

## 第六轮调整：权限层级对齐页面结构 + 系统管理弹窗复用原页面 UI（2026-07-28 深夜）

### 1. 权限层级按实际页面 Tab 分组
- 替换 `groupedPermissions` 逻辑：不再使用 seed permissions 自动派生的 `category`，改用 `PERM_GROUP_MAP` 硬映射
- 分组与 `admin.vue` Tab 层级严格对应：
  - **账号权限**：`admin.manage`, `auth.password.manage`, `system.menu`, `management.menu`
  - **生物识别录入**：`biometric.register`
  - **数据管理**：`employee.view/edit`, `unit.view`, `history.view`
  - **系统工具**：`app.restart`, `authorization.manage`, `settings.basic/advanced`, `debug.command`
  - **设备维护**：`cabinet.*`, `cabinet.view/unlock/unlockAll`, `upgrade.*`, `upgrade.app/firmware`
- 使用 `PAGE_GROUP_ORDER` 强制排序，未分类权限归到"其他"

### 2. 系统管理弹窗：完整复用原页面 UI
- 用户要求：不是新建简单弹窗 UI，而是把原始页面 UI 完整搬到 ModalShell 中，仅改变交互方式（从页面跳转→弹窗）
- **人脸注册弹窗**：完整嵌入 `face.vue` 的 form-panel（顶部图标 + 标题 + 同步行 + 搜索框 + 员工选择 + 添加人脸按钮）+ RecognitionModal
- **指纹注册弹窗**：完整嵌入 `fingerprint.vue` 的 form-panel（图标 + 标题 + 职员ID/姓名输入 + 隐私声明 + 授权按钮）+ RecognitionModal
- **系统授权弹窗**：完整嵌入 `feature.vue` 的 authorization 区（蓝色盾牌图标 + 授权状态/设备编号卡片 + 检测按钮）
- **设备设置弹窗**：完整嵌入 `config.vue` 的全部内容（page-heading 元数据 + 4 节 switch-card 切换器 + 4 个 section-card 表单 + action-bar 保存按钮）
  - sizeClass=`modal-full`，内容可滚动
  - 包含 `SwitchControl` 开关组件、`segmented` 分段选择器、所有字段
  - 保存逻辑调用 `services.saveSettings()` 后自动关闭弹窗

### 3. admin.vue 变更概要
- 新增 import：`RecognitionModal`
- 替换 4 个旧简单弹窗模板（~70 行）→ 4 个完整页面 UI 弹窗（~160 行）
- 替换脚本中系统管理逻辑：新写 face/finger/auth/config 四套完整状态管理函数，每套包含 `init*Form()` 初始化 + 业务操作 + RecognitionModal 状态机
- 替换样式：从旧的 8 行 `.mod-inner`/`.fld` 样式 → ~130 行完整页面样式（包含 search-box, result-list, auth-card, config-switcher, section-card, field-grid, segmented, switch-control, action-bar 等）

**涉及文件**：
- `uniapp/src/pages/admin/admin.vue`（重大修改：4 个弹窗模板 + 脚本 + 样式 + imports）
- `uniapp/src/pages/admin/role-manage.vue`（修改：分组映射）

**验证**：lint 零错误（2 个文件）；JS 语法校验通过；真机未验证。
## 第六轮调整修正：Panel 组件化（2026-07-28 深夜）

用户批评之前手写新 UI "别自由发挥创意"，要求直接把原页面封装成组件调用。

### 新建 4 个 Panel 组件
- `FaceRegisterPanel.vue` — face.vue 原样模板+逻辑+样式，emit `done`
- `FingerprintRegisterPanel.vue` — fingerprint.vue 原样，emit `done`
- `AuthorizationPanel.vue` — feature.vue 授权区原样，emit `done`
- `DeviceConfigPanel.vue` — config.vue 完整提取（SwitchControl + assignForm/buildPayload/validate/save + 全部样式），emit `done`

### 页面更新
- `face.vue`/`fingerprint.vue`：AdminHeader + AdminPageToolbar + `<Panel @done="back" />`
- `feature.vue`：authorization 分支改为 `<AuthorizationPanel />`，移除冗余 auth 变量和函数
- `config.vue`：简化为 `<DeviceConfigPanel @done="goBack" />`

### admin.vue 弹窗精简
- 移除约 280 行手写弹窗模板 + 约 250 行手写状态逻辑 + 约 130 行手写样式
- 改为 `<ModalShell><Panel @done="systemModal = ''" /></ModalShell>`
- `onSysItem` 简化为纯 `systemModal` 赋值

涉及 10 文件（4 new + 6 mod），lint 零错误，真机未验证。

## 设备维护 Tab 图标对齐 + 弹窗本地化（2026-07-28 深夜）

用户要求 admin.vue 设备维护 Tab 中的 7 项菜单图标对齐 engineering.vue，且升级/硬件信息弹窗在 admin 内直接实现（不跳转 engineering）。

### 变更概要

**icon 对齐**：`refresh`/`upload`/`unlock`/`info`/`terminal` → `board-upgrade`/`app-upgrade`/`eject`/`hardware`/`work-card-upgrade`/`command-check`/`main-board`，与 engineering.vue 的 `allMenus` 完全一致。

**分发逻辑**：`handleEngAction` 按 `key` 分发：
- `unlockAll` → 一键弹出确认弹窗
- `board`/`app`/`workCard`/`mainBoard` → 升级弹窗（文件列表 + 进度条 + 「马上升级」按钮）
- `hardware` → 硬件版本号弹窗（图标+标题+内容+确定按钮）
- `command` → navigateTo serial-demo

不再有 `switch` + `default` 统一跳转 engineering.vue。

**新增状态**：`upgradeVisible`/`infoVisible`/`activeAction`/`upgradeFiles`/`selectedFile`/`upgradingProgress`/`upgrading`/`engInfo`；`onMounted` 预加载 `services.getUpgradeFiles()`。

**新增模板**：升级弹窗（~19行）和硬件信息弹窗（~9行），CSS ~20 条规则。

**文档更新**：`docs/管理界面页面层级分析-2026-07-28.md` 新增「设备维护 Tab 实现细节」子章节。

**验证**：lint 零错误；真机未验证。
