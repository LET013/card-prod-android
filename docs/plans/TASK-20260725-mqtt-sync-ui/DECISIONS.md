# TASK-20260725：决策记录

## D-001：先审计和规划，禁止直接开工

状态：已确认

决定：

- 当前阶段只审计现有代码和协议；
- 输出需求、数据流、时序、状态与文件计划；
- 未经再次确认不得修改运行代码。

确认来源：用户明确要求“先审计，做规划，不要直接就动代码，按照agents里面的规划进行”。

## D-002：公开入口不要求管理员模式

状态：已确认

决定：

- 人员同步、人脸同步、指纹同步以及触发入口不强制进入管理员模式；
- 实现时仍必须通过可信WebView Bridge进入Android数据层；
- 不允许Vue直接连接后端。

确认来源：用户明确要求“不一定要进入管理员模式才能触发”。

## D-003：职责范围

状态：已确认

决定：

- 本任务负责前端和Android客户端；
- 后端服务端与串口底层由其他负责人维护；
- 不修改后端服务端、MQTT底层传输和串口负责人代码；
- 只消费现有公开接口、回调和协议能力。

确认来源：用户此前明确“主要负责内容：前端和客户端。后端与串口由其他人负责”。

## D-004：Vue职责解释

状态：设计结论，待用户确认

决定建议：

Vue负责按钮和展示，但实际同步、Map提交、HTTP分页和MQTT发送继续在Android客户端执行。

理由：现有架构规定Android Map是唯一真相，且现有同步实现已经位于`DeviceDataSyncManager`。

## D-005：首页入口形式

状态：待确认

建议：

在首页Header管理员图标左侧增加公开同步图标，点击打开`SyncPanel`。

备选：在首页底部增加文字按钮，但会挤压现有统计和“开始取卡”区域。

## D-006：手动同步默认模式

状态：待确认

建议：

普通公开入口只执行增量同步，不提供全量同步按钮。

理由：全量同步会清空并重建Map，风险更高，不适合作为随手可点的公开动作。

## D-007：状态上报结果文案

状态：待确认

建议：

第一阶段显示“已提交上报请求”，不显示“服务端确认成功”。

理由：现有底层没有向客户端数据层公开`statusReportResp.code/msg`。

## D-008：周期上报策略

状态：待确认

建议：

保留当前周期上报，不在本任务修改10000毫秒本地默认值，也不新增事件驱动防抖策略。

理由：协议只定义报文，没有规定上报周期；本任务先完成公开触发和状态展示。
